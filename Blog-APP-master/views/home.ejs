<%- include("partials/header") %>
    <h1>Home</h1>
    <div>
        <%= Posts[0].content %>
    </div>
    <% for( let index=1; index < Posts.length; index++ ) { %>
        <h1>
            <%= Posts[index].title%>
        </h1>
        <div>
            <%= Posts[index].content.substring(0, 100)+"..." %>
                <a href="/posts/<%= Posts[index]._id %>">Read More</a>
        </div>
        <% } %>
            <br>
            <%- include("partials/footer") %>